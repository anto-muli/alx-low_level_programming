#include <stdlib.h>
/**
 * mod - gives a sum of integer a and b
 * @a: first integer
 * @b: secong integer
 * Return: returns the modulus of integers a and b
 */
int mod(int a, int b)
{
	return (a % b);
}
