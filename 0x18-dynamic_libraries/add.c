#include <stdlib.h>
/**
 * add - gives a sum of integer a and b
 * @a: first integer
 * @b: secong integer
 * Return: returns the sum of integers a and b
 */
int add(int a, int b)
{
	return (a + b);
}
