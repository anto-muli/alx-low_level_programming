 #include <stdlib.h>
/**
  * sub - gives the difference of integer a and b
 * @a: first integer
 * @b: secong integer
 * Return: returns the subtraction of integers a and b
 */
int sub(int a, int b)
{
	return (a - b);
}
