#include <stdlib.h>
/**
 * mul - gives multiplication of integer a and b
 * @a: first integer
 * @b: secong integer
 * Return: returns the multiplication of integers a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
